gold:
  outputs:
    dev:
      type: athena
      region_name: ap-southeast-1
      database: awsdatacatalog
      schema: prod
      s3_data_dir: "s3://{{ env_var('S3_BUCKET') }}/gold/"
      s3_staging_dir: "s3://{{ env_var('S3_BUCKET') }}/athena-query-results/"
      threads: 1
  target: dev
