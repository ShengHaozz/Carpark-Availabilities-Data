[project]
name = "gold"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ShengHaozz", email = "shenghaoong@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dbt-athena-community>=1.11.0",
    "dbt-core>=1.12.2",
]

[project.scripts]
gold = "gold:main"

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"
